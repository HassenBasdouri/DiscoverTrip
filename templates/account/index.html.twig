{% extends 'base.html.twig' %}

{% block title %}Manage Account!{% endblock %}

{% block stylesheets %}
    {{ parent() }}

    <link rel="stylesheet" href="{{ asset('css/account.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <div class="row user-menu-container square">
            <div class="col-md-12 user-details">
                <div class="row spacepurplebg white">
                    <div class="col-md-2 no-pad">
                        <div class="user-pad">
                            <h3>Welcome back, {{ app.user.fullName }}</h3>
                            <div class="user-image">
                                <img src="{{ app.user.username }}" class="img-responsive thumbnail">
                            </div>
                            <h5> Name :{{ app.user.fullName }}</h5>
                            <h5> Name :{{ app.user.fullName }}</h5>
                            <h5> Name :{{ app.user.fullName }}</h5>
                            <h5> Name :{{ app.user.fullName }}</h5>
                            <a class="btn btn-labeled btn-info" href="#">
                                <span class="btn-label"><i class="fa fa-pencil"></i></span>Update
                            </a>
                        </div>
                    </div>
             </div>
         </div>
        </div>
    </div>
{% endblock %}